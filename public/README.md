# public/

Archivos estáticos que se publican tal cual: íconos de la página y los datos del OCR (motor y modelo de
idioma español de tesseract.js), servidos desde la propia página para no depender de otros servidores.
